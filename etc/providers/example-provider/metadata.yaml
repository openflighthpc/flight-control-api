---
required_credentials:
  - PROJECT_NAME
# Map the values of certain fields of this provider's action script results to standardized values.
result_map:
  list_instances:
    state:
      "on":
        - "running"
      "off":
        - "stopped"
